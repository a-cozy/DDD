<Window x:Class="DispApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" 
        Height="480"
        Width="640"
        MinHeight="{Binding WindowHeight}" 
        MinWidth="{Binding WindowWidth}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" prism:RegionManager.RegionName="MenuBarModule" ></ContentControl>
        <ContentControl Grid.Row="2" prism:RegionManager.RegionName="ImageCtrlDispModule" ></ContentControl>
    </Grid>
</Window>
