<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:TXCustomControlLib">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/TXCustomControlLib;component/Themes/TTextBox.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type local:TTextBox}" BasedOn="{StaticResource TextBoxDefaultStyle}" />

    <Style TargetType="{x:Type local:HorizontalMoveThumb}" BasedOn="{StaticResource ThumbDefaultStyle}" />
        <!--<Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HorizontalMoveThumb}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
</ResourceDictionary>
